@model List<UserViewModel>

<thead>
    <tr>
        <th>FullName</th>
        <th>Age</th>
    </tr>
</thead>    

@foreach (var item in Model)
{
    <tr>
        <td>@item.FullName</td>
        <td>@item.Age</td>
    </tr>
}